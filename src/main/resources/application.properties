# Server
server.port=${PORT:8080}

# H2 Database (file mode)
# File-based H2 so resident registrations, complaints, and events survive
# application restarts. Data lives under ./data (gitignored). The previous
# jdbc:h2:mem:vcsmdb URL kept everything in JVM heap and silently destroyed
# all records on every restart or redeploy.
# AUTO_SERVER lets the H2 console and the app share the file concurrently.
# For production use PostgreSQL via docker-compose or set
# SPRING_DATASOURCE_URL / SPRING_DATASOURCE_DRIVER_CLASS_NAME env vars.
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:h2:file:./data/vcsmdb;AUTO_SERVER=TRUE}
spring.datasource.driver-class-name=${SPRING_DATASOURCE_DRIVER_CLASS_NAME:org.h2.Driver}
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
# H2 console config moved to application-dev.properties
# spring.h2.console.enabled=true (dev only)
spring.h2.console.enabled=false
spring.main.allow-bean-definition-overriding=true


# JPA
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Thymeleaf
spring.thymeleaf.cache=false

# Omnidim Voice AI - Replace with your actual API key from omnidim.io
omnidim.api.key=${OMNIDIM_API_KEY}
omnidim.api.base-url=https://api.omnidim.io/v1

# Custom voice model uploads
# voice.model.s3.bucket=
voice.model.s3.prefix=voice-models
voice.model.s3.region=us-east-1

# Default admin seed (only if username+password are set)
security.admin.username=${ADMIN_USERNAME:admin}
security.admin.password=${ADMIN_PASSWORD:}

# Logging
logging.level.com.vcsm=DEBUG

# Voice Biometrics Settings
voice.biometrics.verification.threshold=0.75
voice.biometrics.sample.rate=16000

# Error Handling
server.error.include-message=never
server.error.include-binding-errors=never
server.error.include-stacktrace=never
server.error.include-exception=false

# Swagger UI Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.api-docs.enabled=true

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:your-email@gmail.com}
spring.mail.password=${MAIL_PASSWORD:your-app-password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000


# Webhook Integration for Urgent Escalations (Slack/Teams)
webhook.escalation.url=

# Resilience4j Circuit Breaker Defaults
resilience4j.circuitbreaker.configs.default.slidingWindowSize=10
resilience4j.circuitbreaker.configs.default.failureRateThreshold=50
resilience4j.circuitbreaker.configs.default.waitDurationInOpenState=10s
resilience4j.circuitbreaker.configs.default.permittedNumberOfCallsInHalfOpenState=3
resilience4j.circuitbreaker.configs.default.minimumNumberOfCalls=5

# ML Service Configuration
ml.service.url=${ML_SERVICE_URL:http://localhost:8000}
ml.service.enabled=true

# Twilio Configuration
twilio.account.sid=${TWILIO_ACCOUNT_SID:YOUR_ACCOUNT_SID}
twilio.auth.token=${TWILIO_AUTH_TOKEN}
twilio.phone.number=+1234567890
twilio.webhook.url=http://your-ngrok-url/api/twilio/webhook


# PWA Configuration
pwa.enabled=true
pwa.cache.strategy=cache-first
pwa.offline.page=/offline

# Rate Limiting Settings
rate.limit.default.capacity=10
rate.limit.default.duration=60
rate.limit.default.refill=10
rate.limit.enabled=true

# JWT Configuration
jwt.secret=${JWT_SECRET:}
jwt.expiration=${JWT_EXPIRATION:86400000}
jwt.refresh-expiration=${JWT_REFRESH_EXPIRATION:604800000}

# HMAC Security
security.hmac.secret=${HMAC_SECRET}

# Webhook Integration for Urgent Escalations (Slack/Teams)

# Spring AI OpenAI configuration
spring.ai.openai.api-key=${OPENAI_API_KEY:default-key}
spring.ai.openai.chat.model=gpt-3.5-turbo

# Spring AI Vector Store configuration
spring.ai.vectorstore.pgvector.index-type=HNSW
spring.ai.vectorstore.pgvector.distance-type=COSINE_DISTANCE
spring.ai.vectorstore.pgvector.dimensions=1536
